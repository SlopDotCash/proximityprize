node_modules/
.next/
.vercel/
*.tsbuildinfo

# /public/{mission,skill,codex}.md are generated by scripts/copy-mine.mjs from /mine
# (the single source of truth) and COMMITTED, so they serve at the domain regardless of
# the deploy's build command. To refresh after editing /mine: `npm run build` (or
# `node scripts/copy-mine.mjs`) regenerates them — commit the result.
/out/

/data/research.json
