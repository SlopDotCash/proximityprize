# Allowlist of documented, tracked residual `axiom` declarations.
#
# scripts/forbidden_tokens.py rejects every custom `axiom` in live ArkLib
# source as a laundering token. A *named, deliberately retained* residual —
# an explicit theorem-scope obligation owed to an open tracking issue, kept as
# an `axiom` rather than a `sorry` so it is visible and auditable — is listed
# here to keep CI gate 1 green WITHOUT weakening the gate: any axiom whose
# declared name is NOT in this file still fails the precheck.
#
# Format: one entry per line, `<axiom-name>  # #<issue> short note`.
# The name must be the declared identifier exactly as written at the `axiom`
# site (the simple name, before namespace prefixing). Comments (`#`) and blank
# lines are ignored. Keep this list minimal: every entry is a debt. When the
# owning issue discharges/removes the axiom, delete its line here (the precheck
# warns on stale entries that match no axiom in the tree).
#
# This is route (a) of #111. Routes (b) "discharge" and (c) "convert to
# def : Prop / class" remain preferable long-term and are owned by each
# residual's tracking issue below.
#
large_agreement_set_on_curve_implies_correlated_agreement  # #470 upstream-inherited BCIKS20 curve agreement proof debt
large_agreement_set_on_curve_implies_correlated_agreement'  # #470 upstream-inherited BCIKS20 Johnson-regime curve agreement proof debt
weighted_correlated_agreement_for_parameterized_curves  # #470 upstream-inherited BCIKS20 weighted curve agreement proof debt
weighted_correlated_agreement_for_parameterized_curves'  # #470 upstream-inherited BCIKS20 weighted curve agreement proof debt
weighted_correlated_agreement_over_affine_spaces  # #470 upstream-inherited BCIKS20 weighted affine-space agreement proof debt
weighted_correlated_agreement_over_affine_spaces'  # #470 upstream-inherited BCIKS20 weighted affine-space agreement proof debt
no_selfReciprocal_factor  # #470 Hachi subfield root-orbit factor-swap proof debt
foldKnowledgeStateFunction_toFun_full  # #470 Binius fold RBR toFun_full support-normalization proof debt
foldOracleVerifier_rbrKnowledgeSoundness  # #470 Binius fold RBR soundness probability-normalization proof debt
