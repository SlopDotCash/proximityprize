# #466 novel-math round — lane N3-homogeneous: Hecke points, the space of lattices, and ×2-rigidity, developed to their deaths

**Date:** 2026-07-01 · **Issue:** #466 · **Lane:** N3-homogeneous · **FINALIZED by lane judge 2026-07-02**
**Adjudicated verdict:** **REDUCES_TO_WALL** (self-refutation confirmed by both refuters and by
independent judge re-derivation of every disputed constant; dying step = S4 Kill 2×4, honest
residue = the already-named open Props TPS + D2 coupling; Directions II/III land on the
lone-spike/second-order meta-theorem and on BGK verbatim). Adjudication record, two constant
errata (`mean^{−0.39}` not `^{−0.47}`; `2^146.2` not `2^150`), one sharpening (Sobolev tax
`p^{+3011}` exact, resolving the refuters' 3006-vs-2822 discrepancy in A's favor), and the
DISPROOF-ready paragraph: §9.
**Proposer verdict (upheld):** SELF-REFUTED — all three sub-routes die on exact arithmetic or exact structure.
The lane's positive residue: an exact new dictionary (sections = Hecke points of one `T_p`
correspondence), three sharp depth crossovers that correct the lane's own seed framing, a
Parseval refutation of the "cusp-avoidance" form of the target at depth `r ≥ 5`, a type-error
warning that cheaply kills a family of future proposals, and the identification of the lane's
honest residue with the **already-named** open Props (TPS divisor-equidistribution, D2 pointwise
coupling) — i.e. the route adds **no new open input and no new leverage**.
**Probe:** `scripts/probes/probe_466_novel_n3_hecke_sections.py` →
`scripts/probes/_out_466_novel_n3_hecke_sections.txt` (all identity checks EXACT/MATCH).
**Core status: unchanged — OPEN, ON-BGK.**

Prize point throughout: `n = 2^30`, `β = 4` diagonal `p ≈ 2^120` (`m = 2^90`, `ln p = 83.18`,
`r* = 84`, `d = 2r* = 168`); "true prize field" variant `q ≈ n·2^128 = 2^158` (`m = 2^128`,
`r* = 110`, `d = 220`) given in the table. Both computed; conclusions identical.

---

## 1. The dictionary (exact, proven, probe-checked): the sections ARE Hecke points

Fix `μ_n = ⟨h⟩ ⊂ F_p^×`, `h` of order `n`, and a support `S = {s_1 < … < s_d} ⊂ Z/n`, `d = 2r`.
The depth-`r` wraparounds supported on `S` are the `{0,±1}`-vectors of the **section**

> `L_S(p) := ker(Z^S → F_p, a ↦ Σ_i a_i h^{s_i})`, with `c_S := (h^{s_1}, …, h^{s_d})`.

**Proposition 1 (index and dual — two-line proofs).**
(i) `L_S(p)` has index exactly `p` in `Z^d` (the defining map is surjective since every `h^{s_i}`
is a unit). (ii) The dual lattice is exactly
> `L_S(p)^* = Z^d + Z·(c_S/p)`.
*Proof of (ii):* `⟨c_S/p, a⟩ = (a·c_S)/p ∈ Z` for `a ∈ L_S` by definition, so RHS ⊆ L*; the RHS
contains `Z^d` with quotient generated by `c_S/p`, of order exactly `p` in `(R/Z)^d` (a unit
entry), so covol(RHS) = 1/p = covol(L*); equal covolume + containment ⟹ equality. ∎
(Probe part (1): 1000/1000 membership and integrality spot-checks at n = 16, p = 65537/65617.)

**Proposition 2 (Hecke-point identification).** The index-`p` sublattices of `Z^d` are exactly
the kernels `ker(a ↦ a·c mod p)` for `[c] ∈ P^{d−1}(F_p)`; rescaled by `p^{−1/d}` they form the
standard minuscule Hecke orbit `T_p·[Z^d] ⊂ X_d = SL_d(Z)\SL_d(R)`, `deg T_p = #P^{d−1}(F_p) =
(p^d−1)/(p−1) ≈ p^{d−1}`. Hence
> `x_S(p) := p^{−1/d}·L_S(p)` **is the Hecke point of `T_p[Z^d]` with parameter
> `[c_S] = [h^{s_1} : … : h^{s_d}] ∈ P^{d−1}(F_p)`.**
This completes the SST reformulation (essay §2.3, round-2-clarified) to the group level — the
seed's "Hecke-orbit-like family" is *literally* a subfamily of one Hecke orbit at each `p`.

**Proposition 3 (dilation = projectivization).** The `μ_n`-dilation `b ↦ ub` scales `c_S` by a
global unit, hence acts **trivially** on `[c_S]`. The Hecke fiber sees exactly the
dilation-quotient of the problem: the standing ARC/RESIDUE b-blindness is, in this language, the
projectivization. (Consistency check: form A is dilation-invariant, and the framework expresses
it; but no per-`b` discrimination can ever appear here.)

**Proposition 4 (the only "dynamics" on the family).** On supports: the shift `S ↦ S+1`
multiplies the defining form by the unit `h` — an **isometry** of sections (round-2:
orbit-compression is exact factor-`n` bookkeeping, DISPROOF `466-r2-sst-orbit-compression-cosmetic`).
The multiplier `S ↦ kS` (`k` odd) satisfies the **exact Galois-twist identity**
> `count(kS; h) = count(S; h^k)`
(probe part (3): 60/60 exact) — it is generator-change, not motion. The aggregate count over all
sections is generator-independent, so the multiplier action is **information-free at the
aggregate level**; what remains of it is the round-2 "multiplier residue" (cross-orbit
correlation), untouched here. The acting group `(Z/n)^× ≅ Z/2 × Z/2^{μ−2}` is **virtually
cyclic** — this is load-bearing for §5.

---

## 2. What form A actually says in this language — and the seed framing is wrong past depth ~2

The seed proposed: *"the prize (form A at depth r) = for all but `K^r`-many sections, `x_S(p)`
avoids the Minkowski cusp region."* Developing this honestly **refutes the framing**:

1. **The radius arithmetic.** A full-support ±1 vector of the rescaled section has norm
   `√d·p^{−1/d}`; the Gaussian/Minkowski first-minimum scale of a unimodular rank-`d` lattice is
   `λ_gauss ≈ √(d/2πe)`. Ratio: `√(2πe)·p^{−1/d}` — at depth exactly `r = ln p` this is the
   universal constant **`√(2π) = 2.507`** (probe: 2.519 at r* = 84). Wraparound vectors at prize
   depth are NOT short vectors; they sit deep in the bulk at 2.5× the Minkowski scale.
2. **Three crossovers at the prize point (β = 4; probe table (4)):**
   - `r_avoid ∈ (2,3)`: expected number of wraparound-carrying antipodal-free sections is 0.33 at
     r = 2 but `5.1·10^16` at r = 3 — the "all but `K^r` sections avoid" form is heuristically
     false from **r = 3**.
   - `r_cusp = ln p / ln(2πe) = 29.3`: below it a ±1 vector would be beyond-Minkowski-short
     (genuine cusp event); above it, bulk radius.
   - `r_sat = log₂(p)/2 = 60`: above it EVERY section is expected to carry wraparounds
     (`2^{2r}/p > 1`); at r* = 84 the per-section mean is `2^48`.
3. **Parseval refutation of the avoidance form (r ≥ 5).** Unconditionally
   `E_r = n^{2r}/p + (1/p)Σ_{b≠0}η_b^{2r} ≥ n^{2r}/p`, and the **proven** char-0 ladder caps
   char-0 solutions at Wick. So for `r > β` the char-p solution mass `≈ n^{2r}/p` MUST be present.
   At r = 5: needed mass `2^{180}` tuples vs the avoidance form's budget
   `K^5·(5!)²·2^10·2^10 ≈ K^5·2^{34}` — constant `K` fails by `≈ 2^{146}`. (Caveat, flagged: the
   step "the forced mass sits in bounded-coefficient full-support layers" uses the standard
   low-support/coefficient-≥2 layer bookkeeping of the DC-subtraction chain — routine but not
   re-derived here. From r = 3 the refutation is heuristic-Siegel; from r = 5 it is
   Parseval-forced modulo that bookkeeping.)
4. **The correct statement** (matches dossier form C exactly): at depth `r ≈ ln p`, form A ⟺
   > the total ±1-point count over the `C(n,2r)` Hecke points `{x_S(p)}` equals the Siegel/DC
   > mean `n^{2r}/p` (= `e^{3410}` in tuple units at β = 4) within `K^r·(2r−1)‼·n^r·(r-orderings)`
   > = `e^{2066+83 ln K}` — i.e. **CLT-scale concentration**: relative fluctuation
   > `≤ mean^{−0.39}`, aggregated over sections, NOT per-section avoidance.
   > *(Judge correction, confirming Refuter-A: `e^{2066}/e^{3410} = e^{−1344} = mean^{−0.394}`;
   > independent Stirling `(167)‼·n^{84} = e^{2093.5}` gives `mean^{−0.386}`. The originally
   > printed `−0.47` is not reproducible under any consistent convention pair; the nearest
   > genealogy — mixing an ordered-tuple mean `e^{4107}` with an unordered fluctuation
   > `e^{2093.5}` — gives `−0.490`, the plausible source of the slip. Under other
   > tuple-multiplicity conventions the exponent ranges ≈ `−0.21` to `−0.39`; CLT-scale in every
   > convention, so nothing downstream changes. The probe output line printing `−0.47` is
   > superseded by this note; see §9.2 errata.)*

So the homogeneous route must produce a **variance/second-moment theorem for a deterministic
one-point-per-prime family**, not a non-concentration/cusp-avoidance estimate. This single
correction already forecloses the tools the seed named (nondivergence, cusp excursion,
Eskin–Margulis–Mozes are first-moment/avoidance technology).

---

## 3. Direction I (deployer/∃-form): the binding arithmetic — computed exactly, five kill layers

**The proposed step.** For each fixed `S`, treat `{x_S(p) : p ∈ [X,2X], p ≡ 1 mod n}` as an
equidistributing family in `X_d` with per-modulus rate `p^{−κ}`; union-bound over sections to
find one prime where no section is bad.

**The union-bound demand (the seed's explicit ask, done exactly).** Need per-section bad-prime
density `< 1/C(n,2r)`, so `κ_needed(r) = log_p C(n,2r) = (2r/β)·(1 − (ln 2r − 1)/ln n)`:

| r | 1 | 2 | 3 | 4 | 5 | 10 | 29 | 60 | **84** |
|---|---|---|---|---|---|----|----|----|----|
| κ_needed (β=4) | 0.49 | 0.96 | 1.42 | 1.87 | 2.32 | 4.49 | 12.33 | 24.50 | **33.63** |

(True prize field `q = 2^158`: κ_needed(110) = 32.91; same shape.) The seed's estimate
`2r/β ≈ 42` is corrected by the Stirling term to **33.6**.

**Kill layer 1 — the rate ceiling.** The best conceivable per-modulus smooth-error rate for any
bounded-conductor observable is the square-root barrier `κ = 1/2` (Ramanujan-quality; actually
proven Duke/DFI-type rates are `κ ∈ [1/28, 1/12]`). Deficit at r*: **67×** in the exponent.
Worse: κ_needed(2) = 0.96 already exceeds 1/2, so a *fictional* Ramanujan-rate theorem certifies
**only depth r = 1 — which is Parseval, already unconditional**. The route yields literally
nothing at any depth even under the most generous fictional analytic input.

**Kill layer 2 — the single-section floor.** #primes in the window ≈ `2^84.6` (β=4), so
certifying even ONE section-window pair (zero bad primes) needs error `< 1/#primes`, i.e.
`κ > log_p(#primes) = 0.705` (0.774 at the prize field) — **beyond the square-root barrier
before any union bound**. At κ = 1/2 the method cannot exclude `p^{0.205} ≈ 2^24.6` spurious bad
primes per section.

**Kill layer 3 — measure zero + Sobolev tax.** "Contains an exact ±1 point" is a countable union
of codim-`d` loci in `X_d` — a **measure-zero event**; equidistribution at any rate says nothing
about it without thickening. Thickening to width `δ` needs `δ ≈ p^{−0.2}` for the main term
(`3^d·δ^d ≤ p^{−33.6}`), while any effective statement on `X_168` (dim = 28,223) consumes a
Sobolev norm of order ≥ dim/2 ≈ 14,000 derivatives: error `≥ δ^{−14000}·p^{−κ} = p^{2800−κ}`.
The thickening tax alone overshoots by **three orders of magnitude in the exponent**,
independently of κ. *(Judge sharpening, adopting Refuter-A: the constraint `3^d·δ^d ≤ p^{−33.63}`
actually forces `δ = 3^{−1}·p^{−0.2002} = p^{−0.2134}` — the extra `log_p 3 = 0.0132` term —
so the exact tax is `p^{+3011}`; with the maximally charitable symmetric-space reading
(dim 14,195, s ≈ 7,098) still `p^{+1514}`. The proposer's `p^{2800}` was an understatement;
see §9.2.)*

**Kill layer 4 — the theorem does not exist.** All proven effective Hecke equidistribution
(Clozel–Oh–Ullmo Invent. Math. 144 (2001); Gorodnik–Nevo; Eskin–Oh) averages over the **full
orbit at one prime**, with rates in `deg T_p` — the wrong variable (see the type-error warning,
§7). What Direction I needs is equidistribution of a **deterministic one-point-per-prime**
family. The only proven relatives select points by *quadratic* congruence data
(Duke–Friedlander–Iwaniec, Ann. Math. 141 (1995): roots of `ν² ≡ −1 mod p` — proven via
Weyl-sum → Salié-sum evaluation, not dynamics). For our family the level-1 Weyl marginals are
sums over primes of `e((Σ m_i h_p^{s_i})/p)` — the **degree ≥ 3 congruence-root equidistribution
problem (roots of a fixed polynomial of degree ≥ 3 mod varying p), open**; here the polynomial is
`Φ_{2^30}` of degree `2^29`. So the needed input's *simplest shadow* is already a named open
problem of analytic number theory — and the route needs it with κ > 0.705 (single section),
jointly in d = 168 coordinates, Sobolev-free, at measure-zero exactness, with union budget
κ > 33.6.

**Kill layer 5 — first-order equidistribution is TRUE and is the enemy.** The landed first-moment
result (`E_p[W_r] ≈ n^{2r−4}/(2r)!` — "the average prime is bad") says the p-family DOES
equidistribute at the level of the expected ±1-count: the average prime realizes the **Siegel
mean** (they agree up to `K^r`-level factors; §2 arithmetic). Equidistribution-in-p at first
order is exactly what makes the average prime bad relative to Wick. What the prize needs from
the p-family is the **second moment** — `Var_p(W_r)` = correlations of divisibility across pairs
of relations = equidistribution of prime-divisor mass of pairs of cyclotomic resultants =
**Conjecture TPS verbatim** (essay §2.5, named open Prop). Direction I terminates, exactly, in
the already-named open input.

---

## 4. Direction II (fixed-p, "dynamics in S"): the elementary-identity collapse

**Density.** At fixed p the sections are `C(n,2r)` points out of `deg T_p ≈ p^{d−1}`:
density `2^{4035−20040} = 2^{−16005}` at r* (β = 4). No equidistribution statement penetrates a
subfamily of density below its error rate; ours is astronomically below.

**The full-fiber statistics are exact counting identities — machine-checked.** Over the whole
Hecke fiber `P^{d−1}(F_p)`, with `N(c) = #{ε ∈ {0,±1}^d \ 0 : ε·c ≡ 0}` and
`π_k = #P^k(F_p)`:
> `T1 = Σ_c N(c) = (3^d−1)·π_{d−2}`,
> `T2 = Σ_c N(c)² = 2(3^d−1)·π_{d−2} + ((3^d−1)² − 2(3^d−1))·π_{d−3}`
(for p > 3, F_p-proportional sign vectors are exactly `±ε`). Probe part (2): brute-force MATCH at
(d,p) = (3,251) and (4,31). **Consequence:** "Rogers/Siegel for the Hecke fiber" is finite-field
linear algebra; mean AND variance over the full orbit are exact identities; effective
equidistribution technology at fixed p estimates numbers that are already exact, and adds
nothing. Moreover `T1 > 0` always — the fiber provably contains `≈ 3^d·p^{d−2}` bad points — so
**no full-orbit statement (at any rate, with any test function, via positivity) can certify that
our specific subfamily avoids them**: for f ≥ 1 on the bad set, `deg·∫f` must exceed T1.

**No action to run dynamics on.** The section family is an orbit of nothing usable: the shift is
an isometry (cosmetic, round 2), the multiplier is the Galois twist (Prop 4 — aggregate-
information-free, virtually cyclic), and the μ_n-dilation is projectively trivial (Prop 3). The
subfamily is carved by the multiplicative structure of `h` — i.e., by the problem itself. Every
rigorous instantiation of "use the lattice geometry at fixed p" factors through either (i)
elementary mod-p counting (exact, already known, = the Siegel mean/variance of the full fiber)
or (ii) transference inequalities (Banaszczyk — lossy by `K^r`, admissible, but they convert the
question back into the SST dual-minima statement whose orbit-compression was proven cosmetic in
round 2). Dead.

**ELMV-packet variant (considered and killed).** The multiplier orbit `{x_{kS}(p)}_k` is a
genuine Galois packet of size `≈ n/4 = 2^28 = p^{0.233}` — the correct analogue of the
Duke/ELMV torus packets. But (a) no packet-equidistribution theorem exists on `X_168` (entropy
and subconvexity methods live in small rank; ELMV is for maximal-torus packets in rank ≤ 3-ish
settings), and (b) even granting one, its conclusion is a packet-AVERAGE of the count — the
Galois-average — which is one more average-vs-max failure (F0) plus the same variance regress:
the packet mean is again Siegel-faithful, the deviation is again TPS.

---

## 5. Direction III (×2-rigidity / BLMV): structurally inapplicable, and the rank-1 statement IS BGK

The seed: "μ_n = orbit of the doubling map; the empirical phase measure at scale 1/p as an
almost-×2-invariant measure; effective rigidity gives entropy lower bounds."

**The honest invariance structure.** The phase measure `ν_b = (1/n)Σ_{x∈μ_n} δ_{bx/p}` lives on
the p-point cyclic group `(1/p)Z/Z` and is **exactly invariant under multiplication by μ_n** — a
**rank-1** (cyclic) action. It is NOT ×2-invariant (2 ∉ μ_n generically), and no second
independent multiplication exists:

**The procyclic kill.** Furstenberg/Rudolph–Johnson/BLMV rigidity requires a **non-lacunary
rank-≥2 semigroup** (×a, ×b multiplicatively independent). The available actions here are (i) on
the phase space Z/p: the cyclic group μ_n; (ii) on the exponent space Z/2^μ: the multiplier group
`(Z/2^μ)^× ≅ {±1} × ⟨3⟩` — **virtually (pro)cyclic** (`Z₂^×` mod torsion is procyclic). There is
no multiplicatively independent pair; the ×2×3 hypothesis is **structurally unsatisfiable on
2-power exponent spaces**. (Side observation: for 3-smooth domain sizes `n = 2^a·3^b` the
multiplier group has rank 2 and this door opens a crack — a non-prize curiosity, since the prize
fixes dyadic n; also the b-blindness and F0 fences would still apply.)

**The rank-1 statement is the SOTA.** "A probability measure on Z/p invariant under a
multiplicative subgroup of size ≥ p^δ has small nontrivial Fourier coefficients" — the honest
measure-rigidity statement available — **is the Bourgain–Glibichuk–Konyagin theorem verbatim**
(the sup over b of `ν̂` IS `M(n,p)/n`). Direction III, instantiated rigorously, re-derives the
`n^{1−o(1)}` SOTA with its ineffective `o(1)` and nothing more.

**Rate mismatch, even fictionally.** The prize needs `|∫e dν_b| ≤ √(2 ln p/n) ≈ 2^{−11.3}`
(n = 2^30) — an *exponential-strength* discrepancy. Effective ×a×b results (BLMV, J. Mod. Dyn.
2009) deliver logarithmic-strength savings (`(log)^{−c}`-type). Even granting the (inapplicable)
theorems, the conversion loses everything: `(83)^{−c} ≫ 2^{−11.3}` for any realistic c.

**Entropy accounting (the seed's "convert entropy lower bounds").** Entropy methods lower-bound
the dimension/entropy of the invariant measure; `ν_b` has maximal entropy trivially (uniform on
n atoms). The prize is not an entropy statement — it is a Fourier-coefficient statement at a
SPECIFIC scale, below what entropy sees (entropy is magnitude-blind at the phase level — the AUP
fence in dynamical clothing). The conversion loses everything, as anticipated.

---

## 6. What survives (the lane's positive residue)

1. **The dictionary (Props 1–4)** — exact, elementary, probe-checked. Clean packaging: the SST
   section family is a one-point-per-prime selection from the standard `T_p` Hecke orbit,
   parameter `[c_S] ∈ P^{d−1}(F_p)`; dilation = projectivization; multiplier = Galois twist.
2. **The three crossovers** `r_avoid ∈ (2,3)`, `r_cusp = ln p/ln(2πe) ≈ 29.3`,
   `r_sat = log₂(p)/2 = 60` — sharpen the dossier's form-C discourse: cusp/avoidance language is
   quantitatively wrong for the prize depth; the honest object is CLT-concentration at the
   Siegel mean. Includes the universal radius constant `√(2π)` at depth `r = ln p`.
3. **The Parseval refutation (§2.3)** of the seed's "all but K^r sections avoid" statement at
   `r ≥ 5` (mod standard layer bookkeeping) — future lattice-framed proposals must state the
   variance form or die on arrival.
4. **The type-error warning:** effective Hecke-point rates are exponents of `deg T_p ≈ p^{d−1}`
   (fixed p, full orbit — where everything is elementary anyway); union bounds over p need
   per-modulus exponents of `p`. Conflating them makes the route look ~`p^{80}` stronger than it
   is. Any future "equidistribution beats the union bound" proposal must present the table of §3.
5. **The reduction identity:** Direction I's residue = **TPS** (divisor-mass equidistribution
   over primes) + **D2 pointwise coupling** — both already named. The homogeneous lane is a
   fourth independent arrival at the `r ≈ β` unconditional boundary (after DC-crossover,
   moment-exponent θ, TPS bookkeeping), now from the space of lattices.

## 7. Relation to prior gates (what was already known vs what is new here)

- **J4 kill (2026-06-17):** "effective equidistribution / Ratner / EMV — F0 bulk weak-*, F5
  abelian torus" killed equidistribution *of the frequency torus*. This lane attacked the
  genuinely different target (the section family in `X_{2r}`, which is NOT abelian and HAS a
  spectral gap) — and died differently: wrong variable (deg T_p vs p), wrong form (avoidance vs
  variance), wrong density (2^{−16005}), no theorem for one-point-per-prime.
- **D2 gates:** `_D2RogersSiegelVarianceGate.lean` (coupling is load-bearing) and
  `_D2LowerTailConcentrationGate.lean` (∃-form gains nothing empirically). This note supplies the
  structural counterpart: even with a coupling *shape*, the equidistribution supply side cannot
  reach κ_needed at any depth ≥ 2.
- **Round-2 SST verdict:** orbit-compression cosmetic; this note completes SST's group-theoretic
  reading (Hecke points) and closes its dynamical escape hatches.
- **K2/FHK, γ₂-degeneration, Meta-Theorem:** the lane's conclusions are consistent with all
  standing fences; no fence needed re-litigation.

## 8. The honest bottom line

Every rigorous instantiation of "effective homogeneous dynamics on the wraparound sections"
either (a) computes something elementary (full-fiber counting identities — probe part 2), (b)
demands analytic inputs 67× beyond the square-root barrier with a 3-orders-of-magnitude Sobolev
overdraft on a measure-zero event (deployer table — §3), (c) needs a one-point-per-prime
equidistribution theorem whose simplest shadow (degree ≥ 3 congruence-root equidistribution) is
itself a named open problem, or (d) instantiates measure rigidity in the only available rank-1
form, which IS the BGK theorem (SOTA). The lane is a **success by self-refutation**: it proves
the seed direction cannot close the prize and locates its entire residue in the already-named
TPS + coupling Props. **No new open input. Core unchanged: OPEN, ON-BGK.**

*Proposer session 2026-07-01, lane N3-homogeneous. No Lean claims are made in this note; the two
flagged non-machine steps are (i) the layer bookkeeping in §2.3 (standard, not re-derived) and
(ii) the κ ≤ 1/2 square-root ceiling (a structural barrier statement about smooth-error methods,
not a theorem). Neither can revive the route: each kill layer is independently ≥ 60×
overdetermined. Nothing here is claimed proven beyond the probe-checked identities and the
two-line lattice propositions.*

---

## 9. ROUND ADJUDICATION (lane judge, 2026-07-02) — FINAL

**Verdict: REDUCES_TO_WALL.** Both refuters returned killed = true; the judge independently
re-derived every disputed constant (§9.2). The lane is correct as executed — no step of the
*corrected* chain is mathematically wrong (so not CLOSED_FALSE), and while the constants do die
spectacularly at the prize point, vacuity is the *overdetermining* layer, not the root: even with
fictional constants the required input either does not exist as a theorem or is the target in
disguise, and the lane's honest terminus is the **already-named open core** (Conjecture TPS +
D2 pointwise coupling) plus known kills (BGK verbatim for Direction III; the lone-spike /
second-order meta-theorem for Direction II). That is the definition of REDUCES_TO_WALL.

### 9.1 Refuter record

- **Refuter-A (arithmetic-vacuity): killed = true, dying step S4 (kill layers 1–3).**
  Reproduced the κ_needed table to 4 decimals (0.9618 at r=2; 33.6287 at r*=84, deficit 67.3×;
  prize field 32.9131 at r*=110), the single-section floor 0.7052/0.7736, and *sharpened* the
  Sobolev layer: the thickening actually needed is δ = 3^{−1}·p^{−κ/d} = p^{−0.213} (not the
  proposer's p^{−0.2}: the constraint 3^d·δ^d ≤ p^{−33.63} forces the extra log_p 3 = 0.0132
  term), so on dim X₁₆₈ = 28,223 with s ≈ dim/2 ≈ 14,111 derivatives the tax is ≈ p^{+3006}
  (proposer's "≥ p^{2800}" was a valid understatement), with symmetric-space charitable floor
  ≈ p^{+1512}; even an arbitrarily super-Ramanujan fictional rate below κ ≈ 3000 fails.
  Confirmed the S3 form correction (radius 2.519·λ_gauss = bulk; Parseval deficit 2^146.2 at
  r=5, catching the probe's "~2^150" rounding erratum). Independently re-verified T1/T2
  brute-force at fresh (d,p) = (3,101), (4,13). Found two non-load-bearing constant errors
  (§9.2 errata).
- **Refuter-B (structural-reduction): killed = true, dying step S4 Kill 4 × Kill 2.**
  The required input is a one-point-per-prime effective Hecke equidistribution theorem that
  **does not exist** (COU/Gorodnik–Nevo/Eskin–Oh average the full orbit with rates in deg T_p —
  the wrong variable; the only one-point-per-prime relative, DFI 1995, is quadratic-only via
  Salié sums; the level-1 Weyl shadow is degree-≥3 congruence-root equidistribution at degree
  φ(2^30) = 2^29, a named open problem), AND at the required single-section strength κ ≥ 0.705
  the "equidistribution input" is equivalent to a certified pointwise-in-p statement — i.e. **the
  target in disguise (circularity)** — before the union-bound demand κ ≥ 33.63 even engages.
  Re-derived T1/T2 (fresh brute-force MATCH at (d,p) = (3,17)) and the procyclic kill; verified
  the packet variant dies as F0 + variance regress; confirmed distinctness from the prior J4
  kill (abelian frequency torus ≠ non-abelian section family). Used the proposer's δ = p^{−0.2}
  in the Sobolev layer (tax p^{+2822}) — superseded by A's corrected δ (§9.2). Flagged
  bookkeeping debts (§9.6).

### 9.2 Judge re-derivations (all disputed or load-bearing constants; machine-recomputed 2026-07-02)

| Quantity | Judge value | Adjudication |
|---|---|---|
| κ_needed(r) = log_p C(n,2r), β=4 | 0.4917 / 0.9618 / 1.4209 / 1.8725 / 2.3184 / **33.6287** (r=1,2,3,4,5,84) | proposer + A exact ✓ |
| single-section floor | log₂#primes = 84.62 ⟹ κ > **0.7052** (prize field 122.22 ⟹ **0.7736**) | ✓ (> 1/2 before any union) |
| prize-field κ_needed(110) | **32.9131** | ✓ |
| radius constant at r = ln p | √(2πe)·e^{−1/2} = **√(2π) = 2.5066**; finite-r value 2.5189 at r*=84 | ✓ bulk, not cusp |
| r_cusp | 29.31 (β=4), 38.59 (prize field) | ✓ |
| Parseval budget at r=5 | needed 2^180 vs K⁵·2^{33.8} ⟹ deficit **2^{146.2}** | kb §2.3 correct ✓; **erratum:** probe line "~2^150" is a rounding slip |
| form-C relative fluctuation | e^{2066}/e^{3410.3} = e^{−1344} = **mean^{−0.394}**; independent Stirling (167)‼·n^{84} = e^{2093.5} ⟹ mean^{−0.386} | **erratum confirmed** (both refuters): printed −0.47 wrong; nearest genealogy (ordered mean e^{4107} vs unordered fluctuation e^{2093.5}) gives −0.490 — plausible slip source, not exact. CLT-scale under every convention — non-load-bearing. Corrected in §2.4. |
| Sobolev tax (A/B discrepancy **resolved**) | dim = 168²−1 = 28,223; s = dim/2 = 14,111.5; true δ = 3^{−1}·p^{−0.2002} = p^{−0.2134} ⟹ tax = δ^{−s} = **p^{+3011}** (judge-exact; A's 3006 = rounding of 0.213·14112); proposer-δ = p^{−0.2} gives p^{+2822} (B's figure, undersharp); symmetric-space charitable floor (dim 14,195, s = 7,097.5) = **p^{+1514}**; fictional κ must exceed ≈ **2856–3045** (convention range), ≈ 1548 even maximally charitably | A's bookkeeping correct, adopted with exact recomputation ✓; every value ≥ 3 orders of magnitude beyond any κ — non-load-bearing which convention wins |
| fixed-p density | log₂C(n,168) = 4035.4; 2^{4035−20040} = **2^{−16005}** | ✓ |
| T1, T2 identities | re-derived by (ε, hyperplane) incidence counting: T1 = (3^d−1)π_{d−2} (each ε≠0 ⊥ a π_{d−2}-point projective hyperplane); T2 = diagonal 2(3^d−1)π_{d−2} + off-diagonal (3^d−1)((3^d−1)−2)π_{d−3} (±ε proportionality for p>3 since λε ∈ {0,±1}^d forces λ = ±1); judge brute-force at **fresh** (d,p) = (3,7): 208/1040, (3,13): 364/1352, (4,11): 10640/96160 — all MATCH | ✓ exact; 7 distinct (d,p) across 4 independent sessions; T1 > 0 always ⟹ positivity block stands |
| S6 needed discrepancy | √(2 ln p/n) = **2^{−11.3}** | ✓ vs log-strength BLMV rates |
| multiplier group | (Z/2^μ)^× ≅ {±1}×⟨3⟩, rank 1 mod torsion | ✓ no ×a×b pair exists |

### 9.3 Adjudicated dying step (resolving the refuters' difference of emphasis)

Both refuters die the lane in **S4**; they emphasize different layers, and both are right — the
layers are compatible and jointly overdetermine the death. Under the honesty contract the
**contract-primary** death is Refuter-B's: **S4, Kill 2 × Kill 4** — the required
one-point-per-prime effective Hecke equidistribution input *does not exist*, and at the strength
the route needs (single-section floor κ > 0.7052 > the √-barrier, before any union bound) it is
equivalent to a certified pointwise-per-prime statement, i.e. a prize-shaped input (circularity);
its honest second-moment residue is **Var_p(W_r) = Conjecture TPS verbatim** (Kill 5), plus the
D2 coupling for the transfer. Refuter-A's layers 1–3 (κ_needed(2) = 0.9618 > 1/2 so
fictional-Ramanujan certifies only r = 1 = Parseval; 67× deficit at r*; κ-independent Sobolev tax
p^{+3011} on a measure-zero event, symmetric-space floor p^{+1514}) make the death
exact-arithmetic-overdetermined so that **no rate hypothesis whatsoever revives the route**. The
seed's original avoidance form dies even earlier, at S3 (Parseval-false for r ≥ 5, deficit
2^146.2; the r = 3–4 seam is covered independently by κ_needed(3) = 1.4209 > 1/2).

### 9.4 Kill-landscape mapping (adjudicated; matches both refuters)

- **Direction I** = union-bound tax over C(n,2r) ≈ 2^4035 objects vs per-modulus √-barrier
  (exchangeability gate) + "the average prime is bad" (first-order equidistribution over p is
  TRUE and is the enemy — Kill 5) with residue = **TPS** — plus (f)-shaped circularity at the
  required strength.
- **Direction II** = tetrachotomy door (iv) / second-order meta-theorem (full-fiber moments are
  exact identities — moments ARE the target) + the **lone-spike filter in lattice clothing**
  (T1 > 0 means positivity post-processing of the aggregate certifies nothing about a
  2^{−16005}-density subfamily); packet variant = F0 average-vs-max + variance regress.
- **Direction III** = **BGK-circular verbatim** (the available rank-1 measure-rigidity statement
  IS the BGK theorem = door (ii) SOTA) + AUP fence (entropy magnitude-blind), structurally closed
  by dyadicity (procyclic multiplier group).
- **Prop 3** (dilation = projectivization) = the ARC/RESIDUE b-blindness made structural.
- **Distinct from the J4 kill** (abelian frequency torus, F0/F5): this lane attacked the
  non-abelian section family in X_{2r} and died on different, correctly identified grounds.
  The dictionary is novel *packaging* claiming zero leverage — salvage, not mechanism.

### 9.5 DISPROOF-ready summary (fold into the disproof ledger when created)

> **DISPROOF 466-N3-homogeneous-hecke (2026-07-01/02, adjudicated REDUCES_TO_WALL).** The
> depth-r wraparound sections are literally Hecke points: L_S(p) = ker(a ↦ a·c_S mod p) has
> index p, dual Z^d + Z·(c_S/p), so x_S(p) = p^{−1/d}L_S(p) is the minuscule T_p point with
> parameter [c_S] ∈ P^{d−1}(F_p); dilation = projectivization (b-blindness), shift = isometry,
> multiplier = Galois twist count(kS;h) = count(S;h^k) (probe-exact). Every homogeneous-dynamics
> deployment dies. (∃-form) needs one-point-per-prime effective Hecke equidistribution — a
> theorem that does not exist (proven rates are in deg T_p ≈ p^{d−1}, the wrong variable; only
> quadratic DFI 1995 selects points per-prime; the level-1 shadow is degree-2^29 congruence-root
> equidistribution, open) — at single-section strength κ > 0.7052 (already circular = pointwise
> certification) and union strength κ_needed = log_p C(n,2r) = 33.63 at r* = 84 (vs √-barrier
> 1/2: 67×; fictional-Ramanujan certifies only r = 1 = Parseval; measure-zero + Sobolev tax on
> X₁₆₈ adds p^{+3011}, symmetric-space floor p^{+1514}); honest second-moment residue =
> **Var_p(W_r) = Conjecture TPS verbatim** +
> D2 coupling. (fixed-p) full-fiber mean/variance are exact identities T1 = (3^d−1)π_{d−2},
> T2 = 2(3^d−1)π_{d−2} + ((3^d−1)²−2(3^d−1))π_{d−3}, machine-matched, with T1 > 0 blocking every
> positivity route; the sections are a 2^{−16005}-density subfamily with no acting group.
> (×2-rigidity) (Z/2^μ)^× is virtually cyclic — no rank-2 input exists on dyadic exponent
> spaces; the honest rank-1 statement IS BGK (SOTA, ineffective); rate gap 2^{−11.3} vs
> log-strength. Form correction banked: at prize depth the wraparound vectors sit at
> √(2π)·λ_gauss (bulk, not cusp); the "all but K^r sections avoid" framing is Parseval-false for
> r ≥ 5 (deficit 2^146.2); the honest target is CLT-concentration at the Siegel mean (dossier
> form C), relative fluctuation mean^{−0.39} (corrected from −0.47). No new open input; a fourth
> independent arrival at the r ≈ β boundary, from the space of lattices. Core unchanged: OPEN,
> ON-BGK.

### 9.6 Bookkeeping debts (recorded, not fixed here; no commits in judge session)

1. The round-2 DISPROOF tags (`466-r2-*`) live in kb notes and the essay addendum; no standalone
   `DISPROOF_LOG.md` exists yet (judge-verified by search). §9.5 is written fold-ready.
2. Probe part (1)'s dual-integrality check is logically identical to its ker-membership check
   (⟨c_S/p, a⟩ ∈ Z ⟺ a·c_S ≡ 0 mod p) — a smoke test, not independent evidence; harmless, since
   Prop 1 is a two-line proof (Refuter-B's observation, confirmed).
3. Probe output lines 69/71 (`~2^150`, `mean^{-0.47}`) contain the two errata of §9.2. The output
   file is left as generated (machine artifact); this section is the correction of record.

### 9.7 Bankable sub-results (land regardless of verdict)

1. **The dictionary (Props 1–4)**: sections = one-point-per-prime selections from the minuscule
   T_p Hecke orbit on X_{2r}, parameter [c_S] ∈ P^{d−1}(F_p); dilation = projectivization
   (b-blindness made structural); shift = isometry; multiplier = Galois twist, information-free
   at aggregate. Probe-exact (1000/1000, 60/60).
2. **Three depth crossovers + the universal radius constant**: r_avoid ∈ (2,3),
   r_cusp = ln p/ln(2πe) = 29.3 (38.6 prize field), r_sat = log₂(p)/2 = 60 (79); radius
   √(2π)·λ_gauss at depth r = ln p. Cusp/avoidance language is quantitatively wrong at prize
   depth; the honest object is form C (CLT-concentration at the Siegel mean).
3. **The Parseval filter**: any future "all but K^r sections avoid" lattice framing is dead on
   arrival for r ≥ 5 (deficit 2^146.2 at r = 5; independent κ-arithmetic covers r = 2–4).
4. **The type-error warning + κ_needed table**: Hecke-point rates are exponents of
   deg T_p ≈ p^{d−1}, not of p (conflation inflates a route by ~p^{80}); the table
   (0.49/0.96/…/33.63) is a reusable referee tool for any "equidistribution beats the union
   bound" proposal.
5. **Exact full-fiber identities** T1/T2 ("Rogers/Siegel for the Hecke fiber" = finite-field
   linear algebra), machine-matched at seven distinct (d,p) across four independent sessions:
   proposer (3,251), (4,31); Refuter-A (3,101), (4,13); Refuter-B (3,17); judge (3,7), (3,13),
   (4,11); T1 > 0 = standing positivity block.
6. **The reduction identity**: the lane's entire residue = TPS + D2 coupling, both pre-named —
   a fourth independent arrival at the unconditional r ≈ β boundary, from the space of lattices.
7. **Corrected form-C constant**: relative fluctuation mean^{−0.39} (convention-dependent range
   −0.21…−0.39), replacing the erroneous −0.47.
8. **The sharpened Sobolev fence** (Refuter-A's bookkeeping, judge-exact): measure-zero exactness
   on X₁₆₈ forces δ = 3^{−1}·p^{−κ/d} = p^{−0.2134}, tax p^{+3011} (symmetric-space floor
   p^{+1514}) — reusable against any future thickened-equidistribution proposal on the space of
   lattices at prize dimensions.

**FINAL: REDUCES_TO_WALL. Core unchanged: OPEN, ON-BGK.**
