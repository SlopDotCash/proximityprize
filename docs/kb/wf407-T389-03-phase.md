# wf407 / T389-03 — Tower-recursive phase alignment: EXACT cos=1, structurally INERT

**Thread:** T389-03 (389-T03). **Date:** 2026-06-14. **Verdict:** `walled` (to W2 / BGK / Paley
additive-energy equidistribution). Honesty contract held — no fabricated closure.

## The claim (actionable)

At the worst frequency `b*`, the two half-coset Gauss-period sums are EXACTLY phase-aligned
(`cos = 1.0000`, not asymptotic) in both `p~n²` and prize-shaped `p~n⁴` regimes; the alignment
persists one 2-adic level down. This is "the one mechanism distinguishing worst from average that
the moment hierarchy provably cannot see." Tasks: (1) reproduce exact `cos=1`; (2) formalize as a
named Lean lemma (structural fact, NOT the refuted descent); (3) can alignment + a non-moment input
bound `B = max_{b≠0}‖η_b(μ_n)‖`, or is it inert?

## (1) Reproduced — EXACT cos = 1.00000

`scripts/probes/sweep_A12_phase_align.py` (exact, full coset-rep scan, n=8,16,32,64, both regimes):
- `cos(align A,B) = +1.00000` at `b*` in ALL 8 cases; the UNTWISTED branch `‖A+B‖` realizes the max
  (`untwist_bigger = True` everywhere).
- SPLIT residual `≤ 7e-15`, PARALLELOGRAM residual `≤ 5e-13` (float noise) — both identities exact.
- `A, B` real to `≤ 5e-15` (negation symmetry `−1 = z^{n/2} ∈ μ_n`), so "cos=1" = "A,B same sign".
- Persistence one level down: `cos=+1` in 7/8, **flips to −1** at `n=8,p=73` (fresh confirmation the
  top-level alignment is *forced* — it IS the max — but does NOT robustly persist deeper).

## (2) Formalized — named Lean lemmas, axiom-clean

- `Frontier/Sweep_A12_PhaseAlignmentTower.lean` (pre-existing, axiom-clean): split, twist,
  exact parallelogram `‖A+B‖²+‖A−B‖²=2(‖A‖²+‖B‖²)`, and the alignment characterization
  `untwisted_ge_twisted_iff_align : ‖A−B‖² ≤ ‖A+B‖² ↔ 0 ≤ Re(A·conj B)`.
- `Frontier/WF407_T389_03_PhaseInert.lean` (NEW, this thread, axiom-clean — audit
  `[propext, Classical.choice, Quot.sound]`): the SHARPER inertness lemmas
  - `aligned_branch_ge_rms : 0 ≤ Re(A·conj B) → ‖A‖²+‖B‖² ≤ ‖A+B‖²`
  - `aligned_is_max_branch` : aligned ⟹ `‖A−B‖ ≤ ‖A+B‖` AND `‖A‖²+‖B‖² ≤ ‖A+B‖²`
  - `worst_period_squeezed_of_align` : at `b*`,
    `√(‖A‖²+‖B‖²) ≤ ‖η_{b*}(μ_n)‖ ≤ ‖A‖+‖B‖` (both ends MAGNITUDE-only).

## (3) Verdict — STRUCTURALLY INERT as a downward lever (the key finding)

`scripts/probes/wf407_T389-03-phase_inertness.py` (exact, full coset scan) tests three levers:

- **LEVER 3 (decisive):** at `b*` the worst-case period is ALWAYS the **upper** parallelogram
  branch `‖A‖+‖B‖`, and `‖A+B‖ ≥ √(‖A‖²+‖B‖²)` (Cauchy–Schwarz floor). Alignment selects the
  *maximal* of `{‖A+B‖, ‖A−B‖}` — it is the worst-case **obstruction**, the value that makes `B`
  as LARGE as the magnitudes permit. Alignment + any magnitude-only fact never beats the trivial
  `‖A+B‖ ≤ ‖A‖+‖B‖` ceiling (CS-slack `≥ 0` always). So alignment pushes `B` UP, never down.
- **LEVER 2:** alignment fixes the cross-term SIGN (`+`); its SIZE `Re(A·conj B) = ‖A‖‖B‖` (`cos=1`)
  is the open quantity, governed by `E_2`/equidistribution. Inflation `(‖A‖+‖B‖)²/(‖A‖²+‖B‖²)`
  ranges `[1.52, 2.00]` (→2 at `β=4`, where `‖A‖≈‖B‖`).
- **LEVER 1:** the doubling CEILING `B(n) ≤ c·B(n/2)` holds with `c < 2` (measured 1.28–1.97, →2
  in the prize-shaped `β=4` regime), but tower of `c≈2` over `μ` levels gives `2^μ`, EXPONENTIALLY
  worse than the `√(n log n)` target. This is the dual of the already-refuted descent.

**Why this lands as `walled`, not `no-progress`:** the finding is a *precise localization*. Per
C060 (RESEARCH_SYNTHESIS_407_CONNECTIONS), the magnitude `{‖η_b‖}` is already pinned by proven
moments (`E_2 = 3n²−3n`, Parseval); the only open freedom is the PHASE. T389-03 shows the phase
alignment, while exact and worst-vs-average-distinguishing, supplies the WRONG sign of force: it
saturates the worst case, it does not bound it. The downward force on `B` must come from a bound on
the half-period **magnitudes** `‖A‖,‖B‖` — i.e. the additive energy / equidistribution of `μ_{n/2}`
in `F_p`. That is exactly the **W2 / BGK / Paley-graph wall** (`B ≤ 2√n ⟺ Ramanujan ⟺ Paley Graph
Conjecture`, open; best proven BGK `n^{1−o(1)}`). The alignment route collapses ONTO that wall.

## What remains / new avenues

- The alignment is structurally inert ALONE; the only way it could feed a bound is if a SEPARATE
  theorem bounded `‖A‖‖B‖` (the cross-term size) — which is the energy/equidistribution wall.
- Open sub-direction surfaced: the doubling-ceiling constant `c(β)` (LEVER1) climbs to exactly 2 as
  `β→∞` (`‖A‖→‖B‖`, i.e. the two children equimagnitude). A bound `c(β) ≤ 2 − f(n)` with
  `f(n) = Ω(1/poly)` WOULD give a non-trivial tower — but the data shows `c→2`, so this needs the
  same magnitude (energy) input. Not a free lever.
- The persistence flip (`cos=−1` at `n=8,p=73`) is a clean independent witness of why the
  descent fails: alignment is forced at the top (it is the max by definition) but is genuinely a
  per-level coincidence below, washing out toward generic `±1` behaviour (cf. DISPROOF_LOG
  2026-06-13(2), n=256 generic cofactor).

## Artifacts

- `scripts/probes/wf407_T389-03-phase_inertness.py` — the decisive 3-lever inertness probe.
- `scripts/probes/sweep_A12_phase_align.py` — the cos=1 reproduction (pre-existing, re-run).
- `Research/ProximityPrize/Frontier/WF407_T389_03_PhaseInert.lean` — the 3 named
  inertness lemmas (axiom-clean).
- `Research/ProximityPrize/Frontier/Sweep_A12_PhaseAlignmentTower.lean` — the
  pre-existing structural backbone (split/twist/parallelogram/alignment-iff).
