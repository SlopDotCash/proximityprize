# Late Newton covariance archive

Retain `scripts/probes/_out_bgk_late_newton_covariance.txt`. The default three-cell replay exited 0 and reproduced the archive after newline normalization. Both late steps pass their exact signed covariance-sum identities and Fraction decompositions. The optional extended cells were not replayed.

Independent trial division verifies all three primes. For the fixed order-64 histograms through depth seven, every total is at most C(64,7), whose square is below 2⁶³; this bounds their nonnegative full histogram dot products without overflow. The compressed covariance products separately assert a per-chunk bound. Centered products use Python integers.

An independent check at n=8,p=17 enumerates subsets directly, calculates both consecutive centered collision energies and their transition ratio, and matches the orbit-compressed decomposition at r=5 and r=6. The companion JSON records these exact rational values, complete replay and hashes.

These results concern the listed finite cells. They do not establish an asymptotic Wick deficit, a production inequality, or the Delta Star theorem. Decimal columns display exact fractions. Only the originally unreferenced archive advances the corrected aggregate: 66 of 92 reviewed, 26 remaining.
