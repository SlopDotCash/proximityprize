# R387 unresolved solver outcome

The Z3 full-line probe now reserves exit 0 for all searched branches returning unsat, exit 1 for an independently checked MCA counterexample, and exit 2 for unresolved work. Previously every run without a found counterexample returned success, including timeouts and sat assignments that did not establish the MCA condition.

A real `--free-pivot --timeout-ms 1000` run returned unknown and correctly exited 2. Controlled all-unsat and unknown solver outcomes also produce distinct return values. The constraint model and witness checker are unchanged. No unsatisfiability result or counterexample is established by the timeout test. The full census and retention review remain pending (58/92 completed).
