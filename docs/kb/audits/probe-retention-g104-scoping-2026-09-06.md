# Retention mapping: G104 scoping archive — 2026-09-06

The retained `_out_466_g104_ladder_scoping.txt` is an obsolete proposal, not
an executed threshold calculation. Its producer contained exactly a docstring
and `print(__doc__)`. A full replay matches the archived text after CRLF
normalization. The redundant 1321-byte producer is removed; its two incoming
references now point to the archive. No computation or proposal text is lost.

The separate `probe_466_g104_primitive_concentration_factorial_no_go.py`
passes its complete exact modular replay: the eleven displayed powers have
2048 distinct subset sums, including zero only for the empty subset. Their
11!=39916800 ordered permutations share one sum and are zero-sum-free.
This exceeds the proposed s=13 allowance of 14207588 and the proposed
uniform cap of 4194304. The scoping archive must therefore be read alongside
this refutation, not as a usable production input.

The companion JSON retains hashes, the source/output preservation result,
and the full countercertificate output. This audit does not recompile the
Lean consumer or independently certify the fixed modulus's primality.
The Lean reference edit changes a comment only.

The archive advances the original unreferenced cohort to 51 of 92 reviewed,
leaving 41. The removed producer had direct references and does not increment
that cohort. Of the original 421 inventoried artifacts, 420 remain and one
redundant print-only script has been removed with its output preserved.
