# W11 n=16 archive retention audit

Retain `scripts/probes/_out_w11_c3_kill_n16.txt` with its corrected producer. The full n=16 stage exits zero after restoration of its deleted runtime helper. All 44 count/search-progress rows match the archive after elapsed times are removed. The terminal counts are 38 and 34 for the best spread stacks at primes 65617 and 65633; both searched monomial baselines are 9. Every final spread candidate passes the producer's per-scalar interpolation certificate assertions.

The certificate routine's small-field regression independently enumerates all 17 scalars and all 289 degree-<2 codewords on three n=8 stacks, reproducing bad-set sizes 16,17,17. This verifies those test cases through another method; it does not independently regenerate the larger search witnesses. The archived text stores counts, not the complete word/certificate vectors. Search maxima remain lower bounds, so the archived C=3 refutation verdict is superseded by the corrected unresolved verdict.

The source uses complete k-subset interpolation for each selected candidate, while candidate selection is heuristic. The n=32 stage was not replayed. No all-stack upper bound or production theorem follows from these finite runs. The companion JSON preserves source/helper/archive hashes, command and complete corrected output.

This completes one additional original unreferenced archive: 59/92 reviewed, 33 remaining. The producer already had a direct Lean reference. Three restored helper files are additional to the original inventory, of which 420 artifacts remain after one prior removal.
